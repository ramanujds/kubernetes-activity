apiVersion: v1
kind: Service
metadata:
  name: products-service
  namespace: microservices
spec:
  selector:
    app: products-service
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
